# 🧠LangChain RAG Demo System

一个基于 LangChain 构建的多功能检索增强生成（RAG）演示系统，支持混合检索、多模型调用、工具集成与智能代理架构。适用于快速验证 RAG 应用效果、对比不同 LLM 与向量数据库性能。

## 🔍简介

本项目是一个功能完整的 RAG 演示系统，集成了以下核心能力：

- **RAG（Retrieval-Augmented Generation）**：结合向量检索与大语言模型实现知识增强问答。
- **Ensemble Retriever 混合检索**：聚合来自 `Chroma` 和 `Pinecone` 的检索结果，加权合并 Top-K 输出
- **MCP Server & Client 架构**：
- **Supervisor Agent 主管代理架构**：采用 `langgraph` 实现多智能体协作流程，动态调度任务与工具调用。
- **可扩展性设计**：根据用户意图判断是否需要调用外部工具（如搜索）


适合用于企业级知识库问答、智能客服原型、AI 助手开发等场景。

## 🛠 技术栈

### 🤖 大语言模型（LLMs）

| 模型 | 提供方 | 特点 |
|------|--------|------|
| qwen3-max | 阿里云·通义千问 | 高性能闭源模型，适合复杂推理与长文本生成 |
| xop3qwen1b7 | 科大讯飞 | 星火认知大模型系列，中文理解强，响应快 |
| GLM-4.5-Flash | 智谱 AI | 轻量级高速模型，性价比高，适合实时交互 |
`ps：主要新用户免费`

> 支持在运行时通过配置动态切换模型。

### 🗃 向量数据库（VectorDB）

| 数据库 | 类型 | 特点 |
|-------|------|------|
| Pinecone | 云原生向量数据库 | 托管服务，自动索引优化，适合生产环境 |
| Chroma | 轻量本地向量库 | 易于部署，适合开发测试与小型应用 |

> 可同时连接多个数据库，并通过 Ensemble Retriever 进行结果融合。

### 🔤 嵌入模型（Embeddings）

- **模型名称**: `sentence-transformers/all-MiniLM-L6-v2`
- **来源**: HuggingFace 开源
- **维度**: 384
- **特点**: 小巧高效，跨语言支持良好，广泛用于语义相似度计算


### 工具（Tools）

- **DuckDuckGo Search**  
  提供实时网络搜索能力，补充静态知识库之外的信息，增强时效性回答。

> 后续计划接入：Wikipedia API、ArXiv 论文检索、天气查询等工具。
> 
## 核心功能模块

| 模块               | 功能说明 |
|--------------------|---------|
| RAG Pipeline       | 文档加载 → 分块处理 → 向量化存储 → 查询检索 → 上下文注入 → LLM 回答 |
| EnsembleRetriever  | 并行从 Chroma 和 Pinecone 检索，合并并去重 Top-K 结果 |

---

## 快速开始

### 1. 安装依赖

```bash
推荐使用 Python 3.11 或以上版本。
建议使用虚拟环境：

python -m venv venv

venv\Scripts\activate on Windows
# 或 source venv/bin/activate  # Linux/Mac

pip install -r requirements.txt
```

### 2. 配置环境变量
```bash
cp .env.example .env
编辑 .env 文件，填入 API 密钥和配置项。
```

### 3. 启动服务
```bash
使用 LangGraph Dev 进行可视化调试：
langgraph dev
```

## 📈 后续规划

 - [ ] 支持多模态文档解析（PDF、Word、HTML）
 - [ ] 引入评估模块：Recall@K, MRR, Answer Relevance 评分
 - [ ] 支持 FAISS 作为本地向量引擎选项
 
## 📚参考资料

- [LangChain 官方文档](https://python.langchain.com)
- [LangGraph GitHub](https://github.com/langchain-ai/langgraph)
- [Pinecone Docs](https://docs.pinecone.io)
- [Chroma DB Guide](https://docs.trychroma.com)
- [HuggingFace Embeddings](https://huggingface.co/sentence-transformers)

> Made with ❤️ using LangChain & LangGraph